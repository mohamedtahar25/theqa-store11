# THEQA — دليل الإعداد النهائي (Pixel + Google Sheet)

هذا الملف يشرح **فقط** الإعدادات الأخيرة التي تحتاجها قبل استقبال الزبائن.
كل الخطوات هنا تُعدَّل مرة واحدة فقط.

---

## 1) بيكسل Meta (فيسبوك/إنستغرام)

### أين تجد الكود في المشروع؟
افتح كل ملف من هذه الملفات الثلاثة:
- `index.html`
- `products.html`
- `checkout.html`

في كل ملف، ابحث (Ctrl+F) عن كلمة: `META_PIXEL_ID`

ستجدها في **مكانين** بكل ملف:
```
fbq('init', 'META_PIXEL_ID');
```
و
```
src="https://www.facebook.com/tr?id=META_PIXEL_ID&ev=PageView&noscript=1"
```

### ماذا تفعل؟
1. اذهب إلى [Meta Events Manager](https://business.facebook.com/events_manager)
2. أنشئ Pixel جديد إن لم يكن عندك، وانسخ **رقم البيكسل** (Pixel ID) — رقم فقط مثل `123456789012345`
3. في **كل ملف من الثلاثة**، استبدل `META_PIXEL_ID` بهذا الرقم (يوجد مرتين في كل ملف = 6 استبدالات بالمجموع)

استخدم خاصية "Find & Replace" في محرر الأكواد (VS Code: Ctrl+H) لتسريع العملية على الملفات الثلاثة.

---

## 2) بيكسل TikTok

### أين تجد الكود؟
في نفس الملفات الثلاثة، ابحث عن: `TIKTOK_PIXEL_ID`

ستجدها مرة واحدة بكل ملف:
```
ttq.load('TIKTOK_PIXEL_ID');
```

### ماذا تفعل؟
1. اذهب إلى [TikTok Ads Manager](https://ads.tiktok.com) → Assets → Events → Web Events
2. أنشئ بيكسل جديد وانسخ **Pixel Code/ID**
3. استبدل `TIKTOK_PIXEL_ID` به في الملفات الثلاثة (3 استبدالات بالمجموع)

---

## 3) الأحداث المُفعّلة تلقائيًا (لا حاجة لأي تعديل إضافي)

بمجرد وضع الرقمين أعلاه، هذه الأحداث تُرسل تلقائيًا لكل من Meta و TikTok:

| الحدث | يُطلق عند |
|---|---|
| PageView | فتح أي صفحة |
| ViewContent | فتح تفاصيل منتج |
| AddToCart | الضغط "أضف للسلة" |
| InitiateCheckout | فتح صفحة إتمام الطلب |
| Purchase / CompletePayment | تأكيد الطلب بنجاح |

---

## 4) إرسال الطلبات إلى Google Sheet

### الخطوة أ — أنشئ الشيت
اذهب إلى [sheets.google.com](https://sheets.google.com) وأنشئ شيت جديد. في الصف الأول، أضف هذه الأعمدة بالضبط بنفس الترتيب:

```
التاريخ | رقم الطلب | الاسم | الهاتف | الولاية | البلدية | العنوان | نوع التوصيل | ملاحظات | المنتجات | سعر المنتجات | التوصيل | المجموع
```

### الخطوة ب — أضف كود الاستقبال
1. من داخل الشيت: **Extensions ← Apps Script**
2. سترى صندوق كود فارغ تقريبًا (فيه `function myFunction() {}`)
3. اضغط داخل الصندوق، ثم `Ctrl+A` لتحديد الكل، ثم `Delete` للحذف
4. افتح ملف `google-apps-script.gs` (موجود في هذا المجلد)، انسخ **محتواه بالكامل**
5. الصقه داخل الصندوق الفارغ (`Ctrl+V`)
6. احفظ بـ `Ctrl+S` — سيطلب اسمًا للمشروع، اكتب أي اسم مثل "THEQA Orders"

### الخطوة ج — انشره كـ Web App
1. أعلى يمين الصفحة، اضغط الزر الأزرق **Deploy** ← **New deployment**
2. بجانب "Select type"، اضغط أيقونة الترس ⚙️ واختر **Web app**
3. اضبط:
   - **Execute as**: Me (بريدك)
   - **Who has access**: Anyone
4. اضغط **Deploy**
5. ستظهر نافذة أذونات — اضغط **Authorize access**، اختر حسابك، ثم **Advanced** ← **Go to [اسم المشروع] (unsafe)** ← **Allow**
   (هذا التحذير طبيعي 100%، يظهر لأي سكريبت جديد غير منشور في متجر Google، وهو كودك أنت فقط)
6. بعد الموافقة، ستظهر لك نافذة فيها **Web app URL** — انسخ هذا الرابط كاملاً

### الخطوة د — ضع الرابط في المشروع
افتح `js/products.js`، ابحث عن:
```javascript
const GOOGLE_SHEET_URL = "";
```
وضع الرابط بين علامتي التنصيص:
```javascript
const GOOGLE_SHEET_URL = "https://script.google.com/macros/s/xxxxxxxxxx/exec";
```

### كيف تتأكد أنها تعمل؟
افتح موقعك، أضف منتج للسلة، أكمل طلب تجريبي كامل، ثم افتح الشيت — يجب أن يظهر صف جديد خلال ثوانٍ.
إذا لم يظهر: تأكد أن الرابط منسوخ بالكامل بدون فراغات، وأن "Who has access" مضبوط على **Anyone**.

---

## ملخص سريع (Checklist)

- [ ] استبدلت `META_PIXEL_ID` في الملفات الثلاثة (6 مرات)
- [ ] استبدلت `TIKTOK_PIXEL_ID` في الملفات الثلاثة (3 مرات)
- [ ] أنشأت الشيت بالأعمدة الصحيحة
- [ ] لصقت كود `google-apps-script.gs` في Apps Script ونشرته كـ Web App
- [ ] وضعت رابط الـ Web App في `GOOGLE_SHEET_URL` داخل `js/products.js`
- [ ] وضعت رقم هاتف الدعم في `SUPPORT_PHONE_NUMBER` (نفس الملف) — يظهر للزبون في نافذة التأكيد بعد الطلب
- [ ] جربت طلبًا كاملاً وتأكدت أنه ظهر كصف جديد في الشيت
